#!/usr/bin/env bash

# install Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# install stow
brew install stow
stow terminal --dir $HOME/dotfiles --no-folding
stow osx --dir $HOME/dotfiles --no-folding

brew bundle --global
