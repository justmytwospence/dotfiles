#!/usr/bin/env bash
set -e

stow shell --dir $HOME/dotfiles --no-folding

grep -qF `which zsh` /etc/shells || sudo echo `which zsh` >> /etc/shells
chsh -s `which zsh`

vim +PlugUpgrade +PlugUpdate +quitall

$HOME/.tmux/plugins/tpm/scripts/install_plugins.sh
